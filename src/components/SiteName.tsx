import { Link } from "react-router-dom";

const SiteName = () => {
	return (
		<Link
			to="/"
			className="inline-block bg-gradient-to-r from-pink-500 to-purple-500 bg-clip-text font-black text-transparent"
		>
			cast-pub
		</Link>
	);
};

export default SiteName;
